public class Car {
    String name;

    Car(String name){
        this.name = name;
    }

    // we create a constructor to add the car object to the name attribute
    // once we have done that, we want to pass the car object through the garage class
    // so that we can park the car

}
