public class Garage {

    void park(Car car){
        System.out.println("The " + car.name + " is parked in the garage");

    }

    // we define the park method of the Garage class
    // we pass the car object through the park method parameters
    // we do this in a similar way to how we create a new instance of a class

    // now when the park method is used, the print statement will be ran

}
